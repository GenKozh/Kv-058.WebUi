doctype
head
  title= page
  meta(charset='utf-8')
  link(rel='stylesheet' href='//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css')
  style
    |ul.nav a {
    |text-transform: capitalize; 
    |}
body
  .container
    h1= page
    hr
    ul.nav.nav-pills
      each link in links
        li.nav-item: a.nav-link(href=link)= link
      li: a(href='/new') new
    hr
    //- .well
    //-   p First Name: #{query.name}
    //-   p Last Name: #{query.lname}
    hr
    block content